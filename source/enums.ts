export enum LayerSize {
    XS = 'xs',
    S = 's',
    M = 'm',
    L = 'l',
    XL = 'xl',
    XXL = 'xxl',
}

export enum LayerVariant {
    Transparent = 'text',
    Text = 'text',
    Normal = 'normal',
    Primary = 'primary',
    Secondary = 'secondary',
    Info = 'info',
    Success = 'success',
    Error = 'error',
    Warning = 'warning',
    Link = 'link',
    White = 'white',
    Black = 'black',
    Revert = 'revert',
}

export enum RuleType {
    Synthetic = 'synthetic',
    Natural = 'natural',
    Customize = 'customize',
}

export enum ComputeType {
    Spacing = 'spacing',
}

export enum CompilationCommand {
    Serve = 'serve',
    Build = 'build',
    Development = 'development',
    Production = 'production',
}